## Context

Liu's article includes a "build to delete" section about harness decay. The analysis doc classifies Unbound Force components as decay-resistant or decay-prone. Issue #92 calls for a blog post that honestly addresses this.

## Goals / Non-Goals

### Goals
- Explain the harness decay concept with concrete examples
- Classify Unbound Force components into decay-resistant and decay-prone categories
- Provide a practical pruning methodology
- Be honest about the system's weight

### Non-Goals
- Modifying existing pages or components
- Making predictions about specific model versions
- Custom HTML or styling

## Decisions

**File**: `content/blog/build-to-delete.md` with slug `build-to-delete`.

**Structure**: The bet every component makes → Decay-resistant layers → Decay-prone layers → Pruning methodology → CTA.

**Tone**: Honest and self-critical. This post acknowledges that parts of the system may be overbuilt.

## Risks / Trade-offs

**Risk**: Being too self-critical could undermine confidence in the system. Mitigation: frame decay-resistance as a strength (organizational intent persists) and decay-proneness as healthy self-awareness, not a flaw.
